# Copy this file to .env when running locally.
# Do not commit real API keys.

# Choose one provider. If neither key is set, scripts/02_generate_summary.py
# will generate a local rule-based summary so the report can still render.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-20250514

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

